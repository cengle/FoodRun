require 'spec_helper'

describe RecipeList do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    RecipeList.create!(@valid_attributes)
  end
end
