<h1>Editing <%= @meal_plan.name %></h1>

<% form_for(@meal_plan) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @meal_plan %> |
<%= link_to 'Back', meal_plans_path %>