<h1>Welcome 
<% if(current_user) %> 
<%= current_user.username %> 
<% else %>
New User
<% end %>
</h1>

<% @recipes.each do |recipe| %>
<div class="post">
	<p class="meta"><span class="date"> <%= recipe.created_at.asctime %> Posted by <a href="#">recipe.user</a></p>
	<h2 class="title"><%= link_to recipe.title, recipe %></td></h2>
	<div class="entry">
		<%=recipe.directions%>
	</div>
</div>
<% end %>

<%= will_paginate @recipes %>
